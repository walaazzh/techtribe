<form method="post" action="{{ path('app_centrecollect_delete', {'id': centredecollect.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centredecollect.id) }}">
    <button class="btn">Delete</button>
</form>
