<form method="post" action="{{ path('app_blood_transaction_delete', {'id': blood_transaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blood_transaction.id) }}">
    <button class="btn">Delete</button>
</form>
